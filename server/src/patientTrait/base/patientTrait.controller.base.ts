/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { PatientTraitService } from "../patientTrait.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PatientTraitCreateInput } from "./PatientTraitCreateInput";
import { PatientTraitWhereInput } from "./PatientTraitWhereInput";
import { PatientTraitWhereUniqueInput } from "./PatientTraitWhereUniqueInput";
import { PatientTraitFindManyArgs } from "./PatientTraitFindManyArgs";
import { PatientTraitUpdateInput } from "./PatientTraitUpdateInput";
import { PatientTrait } from "./PatientTrait";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PatientTraitControllerBase {
  constructor(
    protected readonly service: PatientTraitService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PatientTrait",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PatientTrait })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: PatientTraitCreateInput
  ): Promise<PatientTrait> {
    return await this.service.create({
      data: {
        ...data,

        patientId: data.patientId
          ? {
              connect: data.patientId,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,
        name: true,

        patientId: {
          select: {
            id: true,
          },
        },

        severity: true,
        traitType: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PatientTrait",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [PatientTrait] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(PatientTraitFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<PatientTrait[]> {
    const args = plainToClass(PatientTraitFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,
        name: true,

        patientId: {
          select: {
            id: true,
          },
        },

        severity: true,
        traitType: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PatientTrait",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PatientTrait })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: PatientTraitWhereUniqueInput
  ): Promise<PatientTrait | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,
        name: true,

        patientId: {
          select: {
            id: true,
          },
        },

        severity: true,
        traitType: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PatientTrait",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PatientTrait })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: PatientTraitWhereUniqueInput,
    @common.Body() data: PatientTraitUpdateInput
  ): Promise<PatientTrait | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          patientId: data.patientId
            ? {
                connect: data.patientId,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,
          name: true,

          patientId: {
            select: {
              id: true,
            },
          },

          severity: true,
          traitType: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "PatientTrait",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PatientTrait })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: PatientTraitWhereUniqueInput
  ): Promise<PatientTrait | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,
          name: true,

          patientId: {
            select: {
              id: true,
            },
          },

          severity: true,
          traitType: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
